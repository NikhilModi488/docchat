# =========================================================================
# Document RAG Chatbot — backend dependencies
# PINNED to the stack proven to work on this Windows/anaconda machine.
# Do NOT bump torch past 2.2.2 (c10.dll WinError 1114) — that forces the
# transformers/sentence-transformers/tokenizers/hub pins below.
# =========================================================================

# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
sse-starlette==2.1.3
slowapi==0.1.9

# --- Auth ----------------------------------------------------------------
bcrypt>=4.2
python-jose[cryptography]==3.3.0

# --- Database ------------------------------------------------------------
SQLAlchemy==2.0.36

# --- LangChain (0.2.x line — compatible with the pinned torch stack) -----
langchain==0.2.16
langchain-core==0.2.40
langchain-community==0.2.16
langchain-text-splitters==0.2.4
langchain-huggingface==0.0.3
langchain-ollama==0.1.3

# --- ML / embeddings / reranker (PINNED — torch 2.2.2 compatible) --------
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
transformers==4.41.2
sentence-transformers==2.7.0
tokenizers==0.19.1
huggingface-hub==0.33.4
numpy>=1.26,<2.0

# --- Vector store + hybrid search ----------------------------------------
faiss-cpu==1.8.0
rank-bm25==0.2.2

# --- Document loaders ----------------------------------------------------
pypdf==4.3.1
pymupdf==1.24.10
python-docx==1.1.2
python-pptx==1.0.2
markdown==3.7

# --- OCR (optional; needs the Tesseract binary installed separately) -----
pytesseract==0.3.13
Pillow==10.4.0

# --- Multilingual --------------------------------------------------------
langdetect==1.0.9

# --- Evaluation (optional; heuristic fallback used by default) -----------
# ragas==0.1.21   # uncomment to enable full RAGAS (heavy; needs a judge LLM)

# --- Testing -------------------------------------------------------------
pytest==8.3.3
httpx>=0.27
